Build and run this Spring app from THIS directory (the one containing this file and "src/main/java"), not from nested subfolders "employee/employee" or "employee/employee/employee" — those are duplicate trees and are not the maintained source. Attendance rows are read/written via com.example.employee.model.AttendanceLog to the "attendance" table (see AttendanceLog.java and sql/verify_attendance_alignment.sql).
