# Copy to application-prod.properties (do not commit) or set the same keys as environment variables.
# Database
# DB_URL=jdbc:mysql://host:3306/eac_hr_db
# DB_USERNAME=
# DB_PASSWORD=
#
# Mail (Gmail: use an App Password, not your normal password)
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=
# MAIL_PASSWORD=
#
# New hires get default portal password; require change via IT policy or a future "must change" screen.
# Schedule regular MySQL backups and test restores (mysqldump or managed backup).
